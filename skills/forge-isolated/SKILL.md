---
name: forge-isolated
description: "Forge changeset inside a new worktree."
disable-model-invocation: true
---

# forge-isolated — Isolated Worktree Construction Pipeline

Execute `/worktree` to create `.agents/worktree/<branch>`, then execute `/forge` inside it.
